<span class="user_profile"><h2><%=@user.name.capitalize%>'s Profile</h2></span>

<div class="timeline">
  <h3>Timeline</h3>
    <ul>
      <% @user.posts.each do |post| %>
      <%= post.content %>
      <%= post.created_at %>
      <% end %>
    </ul>
</div>
