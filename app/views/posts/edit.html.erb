<div class="edit-post">
  <%= form_for :post, url: posts_path(@post), method: :patch do |p| %>

  <p>
    <%= p.label :content %><br>
    <%= p.text_area :content %>
  </p>

  <p>
    <%= p.submit %>
  </p>

  <% end %>

  <%= link_to 'Back', post_path %>

</div>
